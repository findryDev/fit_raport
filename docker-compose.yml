services:
  web:
    build: .
    ports:
      - "5100:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key-change-me}
      DATABASE_URL: sqlite:///./data/fit_raport.db
    volumes:
      - fit_raport_data:/app/data
    restart: unless-stopped

volumes:
  fit_raport_data:
